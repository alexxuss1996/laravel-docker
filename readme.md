# Laravel local environment with Docker
## Requirenments
* Docker
* Docker Compose
* PHP >= 7.2
* Composer

Open terminal and in  **src/** directory run:
```bash
	composer install
``` 
then, in root directory: 
```bash 
	docker-compose up -d
```



